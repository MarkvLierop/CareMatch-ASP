@using CareMatch.Models
@{
    ViewBag.Title = "AccountOverzicht";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AccountOverzicht</h2>

@if (ViewBag.GebruikerList.Count != 0)
{
    @foreach (Gebruiker gebruiker in ViewBag.Gebruikerlist)
    {
        
    }
}